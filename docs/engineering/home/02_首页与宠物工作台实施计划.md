# 首页与宠物工作台实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成毛伙伴首页前后端可扩展骨架，覆盖普通用户宠物主体页、新用户空态、认证商家多宠工作台、宠物事件底座、窝次追溯边界、首页聚合接口和质量门禁。

**Architecture:** 后端采用 Rust workspace crates + 分层架构，新增 pet/home 相关 domain/application/infrastructure/http crates；iOS 采用 Home Feature 下的 Domain/Data/Stores/Presentation 分层，`HomeRootScreen` 只组合 section views；首页通过 `HomeDashboardSnapshot` 聚合读模型驱动。

**Tech Stack:** Rust 2024、axum、sqlx、PostgreSQL、uuid、Swift 6.4、SwiftUI、UIKit、MaohuobanDesignSystem、MaohuobanDiagnostics。

---

### Task 1: 工程文档与边界固化

**Files:**
- Create: `docs/engineering/home/00_首页与宠物工作台设计文档.md`
- Create: `docs/engineering/home/01_首页与宠物工作台Goal进度追踪.md`
- Create: `docs/engineering/home/02_首页与宠物工作台实施计划.md`

- [x] 写首页定调、边界、身份形态和模块职责。
- [x] 写商家记录、窝次追溯、事件账本、证据快照边界。
- [x] 写 UUID ID 策略和后续演进口径。
- [x] 写前后端分层、接口契约和测试策略。
- [ ] 提交 docs-only commit。

### Task 2: 后端测试先行

**Files:**
- Create: `maohuoban-rust/tests/home_contract.rs`
- Create: `maohuoban-rust/crates/maohuoban-home-domain/src/lib.rs`

- [ ] 写首页 dashboard 契约测试，断言 `/api/v1/home/dashboard` 返回 `首页已加载`。
- [ ] 写普通用户快照测试，断言当前宠物、照护、时间线 section 存在。
- [ ] 写新用户空态测试，断言无宠物时返回创建宠物主操作。
- [ ] 写商家工作台快照测试，断言多宠状态和窝次入口存在。
- [ ] 先运行测试并确认失败原因是功能未实现。

### Task 3: 后端宠物与首页分层实现

**Files:**
- Create: `maohuoban-rust/crates/maohuoban-pet-domain/**`
- Create: `maohuoban-rust/crates/maohuoban-pet-application/**`
- Create: `maohuoban-rust/crates/maohuoban-pet-infrastructure/**`
- Create: `maohuoban-rust/crates/maohuoban-pet-http/**`
- Create: `maohuoban-rust/crates/maohuoban-home-domain/**`
- Create: `maohuoban-rust/crates/maohuoban-home-application/**`
- Create: `maohuoban-rust/crates/maohuoban-home-http/**`
- Modify: `maohuoban-rust/Cargo.toml`
- Modify: `Cargo.toml`
- Modify: `maohuoban-rust/src/lib.rs`

- [ ] 建立 `PetProfile`、`PetEvent`、`Litter`、`PetRelationship`、`MerchantDashboardSummary` 基础模型。
- [ ] 建立 `HomeDashboardSnapshot`、`HomeSection`、`HomeIdentity` 读模型。
- [ ] 建立 application ports，首页只依赖宠物 / 商家摘要端口。
- [ ] 第一阶段使用内存或开发 seed repository 返回可验证快照。
- [ ] 接入 `/api/v1/home/dashboard` 路由。
- [ ] 确保返回统一 API response 和中文 message。
- [ ] 运行 `cargo test --workspace` 和 `cargo clippy --workspace --all-targets`。

### Task 4: 数据库迁移与持久化准备

**Files:**
- Create: `maohuoban-rust/migrations/0005_pet_home_baseline.sql`
- Modify: `docs/engineering/home/01_首页与宠物工作台Goal进度追踪.md`

- [ ] 创建 `pet_profiles`、`pet_events`、`pet_relationships`、`litters` 基线表。
- [ ] 使用 PostgreSQL `uuid` 类型和必要索引。
- [ ] 为事件时间线创建 `pet_id + occurred_at desc` 索引。
- [ ] 为商家多宠状态创建 `merchant_id + status` 索引。
- [ ] 记录迁移验证结果。

### Task 5: iOS 首页测试先行

**Files:**
- Create: `maohuoban/maohuobanTests/HomeDashboardStoreTests.swift`
- Create: `maohuoban/maohuoban/Features/Home/Domain/**`

- [ ] 写 DTO 解码测试，断言后端 dashboard JSON 可解码。
- [ ] 写 Store 加载测试，断言 loading -> loaded 状态流。
- [ ] 写空态测试，断言无宠物时主操作为创建宠物。
- [ ] 写商家快照测试，断言商家 section 可识别。
- [ ] 先运行测试并确认失败原因是类型或行为未实现。

### Task 6: iOS 首页分层实现

**Files:**
- Create: `maohuoban/maohuoban/Features/Home/Domain/**`
- Create: `maohuoban/maohuoban/Features/Home/Data/**`
- Create: `maohuoban/maohuoban/Features/Home/Stores/**`
- Create: `maohuoban/maohuoban/Features/Home/Presentation/Sections/**`
- Modify: `maohuoban/maohuoban/Features/Home/Presentation/HomeRootScreen.swift`

- [ ] 建立 `HomeDashboardSnapshot` 前端模型和 DTO mapper。
- [ ] 建立 `HomeRepository` 和 API client 适配。
- [ ] 建立 `@MainActor @Observable HomeDashboardStore`。
- [ ] 将 `HomeRootScreen` 改为组合 section views。
- [ ] 接入宠物主卡、今日照护、提醒、快捷动作、今日伙伴、最近时间线、商家看板、空态。
- [ ] 所有颜色、字体、间距、圆角走 `MaohuobanDesignSystem` token。
- [ ] 用户可见文案在非 View 类型使用 `LocalizedStringResource`。
- [ ] 确保 View 渲染路径无网络、缓存、UserDefaults、全局状态写入。

### Task 7: 端到端验证

**Files:**
- Modify: `docs/engineering/home/01_首页与宠物工作台Goal进度追踪.md`

- [ ] 运行 `cargo test --workspace`。
- [ ] 运行 `cargo clippy --workspace --all-targets`。
- [ ] 运行 iOS Debug build。
- [ ] 运行 DesignSystem test。
- [ ] 启动后端并用 curl 验证 `/api/v1/home/dashboard`。
- [ ] 使用模拟器截图复核首页首屏。
- [ ] 更新 Goal 进度追踪中的证据。
